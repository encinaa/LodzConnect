class TestVO():
    def __init__(self, idTest, titulo, descripcion, fechaLimite, preguntas):
        self.idTest = idTest
        self.titulo = titulo
        self.descripcion = descripcion
        self.fechaLimite = fechaLimite
        self.preguntas = preguntas